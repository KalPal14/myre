export const ROOT_OPTIONS_ROUTE = 'options.html';

export const OPTIONS_ROUTES = {
	userInfo: '?tab=user-info',
	colors: '?tab=colors',
	pages: '?tab=pages',
};

export const FULL_OPTIONS_ROUTES = {
	userInfo: ROOT_OPTIONS_ROUTE + OPTIONS_ROUTES.userInfo,
	colors: ROOT_OPTIONS_ROUTE + OPTIONS_ROUTES.colors,
	pages: ROOT_OPTIONS_ROUTE + OPTIONS_ROUTES.pages,
};
